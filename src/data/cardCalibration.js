// GENERATED by scripts/calibrate-card.mjs — do not edit.
// Measured 2026-09-11 from 112 rated accounts (profiles.stats.catStats via computeCard().rawAccuracy — the card's OWN counted answers (chaos excluded), every account whose card is rated, prod blcisypmngimqkwxrrdm. ⚠️ NOT daily_all_answers: that sample is Daily 7 only and reads ~4 points lower (median .5306 vs .5750), so a curve built on it would sit the whole population too low.).
// Refresh: see the header of scripts/calibrate-card.mjs.
//
// ANCHORS: [weighted accuracy, rating]. ratingFromAccuracy() interpolates
// linearly between them. MEDIAN is the population's median accuracy and is the
// prior every new player's overall starts from (weight PRIOR_WEIGHT answers).
export const CALIBRATION = {
  measured: "2026-09-11",
  n: 112,
  median: 0.5746,
  anchors: [[0,52],[0.3,60],[0.3957,67],[0.5014,72],[0.5746,77],[0.6539,83],[0.7204,88],[0.7981,93],[1,99]],
  // The model (Alex, 2026-09-09): rating = 100 × mean(correct ? MULT[diff] : 0),
  // shrunk toward BASELINE. The anchors above are the measured population in
  // plain accuracy, kept for reference and for the calibration tests.
  mult: {"easy":1,"medium":1.25,"hard":1.5},
  avgMult: 1.2552,
  baseline: 0.7212,
};
